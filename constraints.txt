# pip 约束文件：任何直接/传递依赖都不得改动 torch 版本
# 背景：aarch64 的 torch 2.12 wheel 为纯 CPU 构建；2.13+ 会连带拉取约 3GB 的 CUDA 依赖，
# 且上游 fast-browser-use[torch] 的版本范围（torch>=2.6,<3）放任 pip 把 torch 升到最新，
# 造成镜像体积与内存暴涨。通过 pip install -c 强制钉住。
torch==2.12.*
